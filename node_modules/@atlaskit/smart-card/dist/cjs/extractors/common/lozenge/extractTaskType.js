"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.extractTaskType = void 0;
var _linkExtractors = require("@atlaskit/link-extractors");
var extractTaskType = exports.extractTaskType = function extractTaskType(jsonLd) {
  // TODO: Remove once Jira migrate to the correct type.
  var taskType = jsonLd['atlassian:taskType'] || jsonLd.taskType;
  if (taskType) {
    if (typeof taskType === 'string') {
      return {
        id: taskType
      };
    } else if (taskType['@type'] === 'Link') {
      return {
        id: taskType.href
      };
    } else {
      return {
        id: taskType['@id'],
        name: taskType.name,
        icon: taskType.icon && (0, _linkExtractors.extractUrlFromIconJsonLd)(taskType.icon)
      };
    }
  }
};