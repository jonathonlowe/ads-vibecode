const createStatusUpdateRequest = (request, id) => ({
  providerKey: request.providerKey,
  action: {
    ...request.action,
    payload: {
      newStatusId: id
    }
  }
});
export default createStatusUpdateRequest;