export const combineActionOptions = ({
  actionOptions,
  platform
}) => {
  if (typeof actionOptions !== 'undefined') {
    return actionOptions;
  }
  if (platform === 'mobile') {
    return {
      hide: true
    };
  }
  let exclude = [];
  return {
    hide: false,
    exclude
  };
};