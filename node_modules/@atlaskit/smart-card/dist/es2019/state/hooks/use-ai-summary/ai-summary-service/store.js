import { LRUMap } from 'lru_map';
const AI_SUMMARY_CACHE_SIZE = 100;

// Contains cached mapping between url and an AISummaryService.
export const AISummariesStore = new LRUMap(AI_SUMMARY_CACHE_SIZE);