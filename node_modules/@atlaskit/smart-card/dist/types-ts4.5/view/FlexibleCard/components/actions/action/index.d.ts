import React from 'react';
import { type ActionProps } from './types';
/**
 * A base action that can be triggered with an on click.
 * @internal
 * @param {ActionProps} ActionProps - The props necessary for the Action.
 */
declare const Action: ({ as, appearance, content, isLoading, onClick, size, testId, icon, iconPosition, spaceInline, tooltipMessage, tooltipOnHide, hideTooltip, hideTooltipOnMouseDown, style, asDropDownItem, className, isDisabled, href, ariaLabel, wrapper: Wrapper, }: ActionProps) => React.JSX.Element | null;
export default Action;
