import { type AISummaryServiceInt, type AISummaryServiceProps, type AISummaryState, type StateSetter } from './types';
export declare class AISummaryService implements AISummaryServiceInt {
    state: AISummaryState;
    private config;
    private url;
    private ari?;
    private locale?;
    private subscribedStateSetters;
    private onStart?;
    private onSuccess?;
    private onError?;
    constructor(props: AISummaryServiceProps);
    private getRequestUrl;
    private fetchStream;
    summariseUrl(): Promise<AISummaryState>;
    subscribe(stateSetter: StateSetter): () => void;
}
