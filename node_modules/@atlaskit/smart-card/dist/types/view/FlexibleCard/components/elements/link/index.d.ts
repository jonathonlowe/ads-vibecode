/// <reference types="react" />
import { type LinkProps } from './types';
/**
 * A base element that represent an anchor.
 * @internal
 * @param {LinkProps} LinkProps - The props necessary for the Link element.
 * @see LinkIcon
 */
declare const Link: ({ hideTooltip, maxLines, name, className, size, testId, text, theme, url, onClick, target, }: LinkProps) => JSX.Element;
export default Link;
