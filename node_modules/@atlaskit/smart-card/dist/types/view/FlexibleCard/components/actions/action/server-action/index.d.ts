import React from 'react';
import type { ServerActionProps } from './types';
declare const ServerAction: ({ action, onClick, onError: onErrorCallback, ...props }: ServerActionProps) => React.JSX.Element;
export default ServerAction;
