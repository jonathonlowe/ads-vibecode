import React from 'react';
import { type MediaStore as MediaApi, type MediaClientConfig } from '@atlaskit/media-client';
import { type MediaStore } from '@atlaskit/media-state';
export interface MockedMediaClientProviderProps {
    children: React.ReactNode;
    mockedMediaApi: Partial<MediaApi>;
    mediaStore?: MediaStore;
    mediaClientConfig?: Partial<MediaClientConfig>;
}
export declare const mockedMediaClientConfig: {
    authProvider: () => Promise<{
        clientId: string;
        token: string;
        baseUrl: string;
    }>;
};
export declare const MockedMediaClientProvider: ({ children, mediaStore, mockedMediaApi, mediaClientConfig, }: MockedMediaClientProviderProps) => React.JSX.Element;
