import type { FeatureFlagValue } from '../types';
export declare const shouldRedactValue: (value: any) => boolean;
export declare const redactValue: (featureFlagValue: FeatureFlagValue) => FeatureFlagValue;
