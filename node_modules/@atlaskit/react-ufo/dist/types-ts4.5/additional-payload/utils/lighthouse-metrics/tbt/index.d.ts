import type { PerformanceEntryBuffer } from '../utils/buffer';
export declare const getTBT: (start: number, stop: number, buffer: PerformanceEntryBuffer) => {
    total: number;
    observed: number;
};
