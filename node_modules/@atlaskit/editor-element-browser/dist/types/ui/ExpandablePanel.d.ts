import React from 'react';
import type { ItemData } from './ItemType';
export interface ExpandablePanelProps {
    id: string;
    label: string;
    items: ItemData[];
    hasSeparator?: boolean;
    attributes?: {
        new?: boolean;
    };
    onItemSelected?: (index: number, categoryId: string) => void;
    onViewAllSelected?: (categoryId: string) => void;
}
export declare const ExpandablePanel: React.MemoExoticComponent<({ id, label, items, hasSeparator, attributes, onItemSelected, onViewAllSelected, }: ExpandablePanelProps) => React.JSX.Element>;
