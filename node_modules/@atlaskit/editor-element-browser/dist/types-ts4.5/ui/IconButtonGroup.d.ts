import React from 'react';
import type { GroupData } from './ItemType';
export interface IconButtonGroupProps extends GroupData {
    hasSeparator?: boolean;
    onItemSelected?: (index: number, categoryId: string) => void;
}
export declare const IconButtonGroup: React.MemoExoticComponent<({ id, label, items, hasSeparator, onItemSelected }: IconButtonGroupProps) => React.JSX.Element>;
