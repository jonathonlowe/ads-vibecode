import { type AnalyticsWebClient } from './types';
import { type GasPayload, type GasScreenEventPayload } from '@atlaskit/analytics-gas-types';
import type Logger from './helpers/logger';
type GasEvent = GasPayload | GasScreenEventPayload;
export declare const sendEvent: (logger: Logger, client?: AnalyticsWebClient | Promise<AnalyticsWebClient>) => (event: GasEvent) => void;
export {};
