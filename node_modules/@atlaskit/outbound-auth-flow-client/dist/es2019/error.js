export class AuthError extends Error {
  constructor(message, type) {
    super(message);
    this.message = message;
    this.type = type;
    this.name = 'AuthError';
    this.type = type;
    this.message = message;
  }
}