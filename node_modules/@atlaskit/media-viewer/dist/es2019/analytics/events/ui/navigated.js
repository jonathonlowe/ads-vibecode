import { isFileIdentifier } from '@atlaskit/media-client';
export const inputFromSource = source => {
  return {
    mouse: 'button',
    keyboard: 'keys'
  }[source];
};
export const createNavigatedEvent = (direction, source, identifier) => ({
  eventType: 'ui',
  action: 'navigated',
  actionSubject: 'file',
  actionSubjectId: direction === 'next' ? 'next' : 'previous',
  attributes: {
    fileAttributes: {
      fileId: isFileIdentifier(identifier) ? identifier.id : ''
    },
    input: inputFromSource(source)
  }
});