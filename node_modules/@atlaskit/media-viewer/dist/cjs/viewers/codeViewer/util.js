"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.DEFAULT_LANGUAGE = void 0;
exports.getFormat = getFormat;
exports.normaliseLineBreaks = exports.lineCount = void 0;
var DEFAULT_LANGUAGE = exports.DEFAULT_LANGUAGE = 'c';

/*
 * Primarily used for the header in the viewer. Based on the language name, render the appropriate header name
 * i.e .py file would be 'python', whilse .msg would be 'email'
 */
function getFormat(language, ext) {
  // some formats have a language we pickfor the code block component,
  // but this may be a bit vague, so we can override here where needed
  switch (ext) {
    case 'csv':
    case 'log':
      return ext;
    default:
      // default value to show is just show language
      // uncapitalised as mediatypes such as doc/audio are also uncapitalised for headers
      return language;
  }
}
var lineCount = exports.lineCount = function lineCount(text) {
  return normaliseLineBreaks(text).split(/\n/).length;
};
var normaliseLineBreaks = exports.normaliseLineBreaks = function normaliseLineBreaks(text) {
  return text.replace(/\r\n/g, '\n').replace(/\r/g, '\n');
};