import { isFileIdentifier } from '@atlaskit/media-client';
export var getIdentifierCollection = function getIdentifierCollection(identifier, defaultCollectionName) {
  return isFileIdentifier(identifier) ? identifier.collectionName || defaultCollectionName : undefined;
};