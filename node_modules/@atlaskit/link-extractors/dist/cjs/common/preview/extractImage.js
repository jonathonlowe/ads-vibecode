"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.extractImage = void 0;
var _url = require("../url");
var extractImage = exports.extractImage = function extractImage(jsonLd) {
  var image = jsonLd.image;
  if (image) {
    if (typeof image === 'string') {
      return image;
    } else if (image['@type'] === 'Link') {
      return (0, _url.extractUrlFromLinkJsonLd)(image);
    } else if (image['@type'] === 'Image') {
      if (image.url) {
        return (0, _url.extractUrlFromLinkJsonLd)(image.url);
      }
    }
  }
};