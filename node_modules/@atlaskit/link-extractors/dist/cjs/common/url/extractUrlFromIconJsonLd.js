"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.extractUrlFromIconJsonLd = void 0;
var _extractUrlFromLinkJsonLd = require("./extractUrlFromLinkJsonLd");
/**
 * Extracts the URL from an icon in JSON-LD format.
 *
 * If the icon is a string, it is assumed to be a URL and is returned as is.
 * If the icon is of type Link or the url property exists, the url is extracted recursively.
 * @returns URL string if one is found, otherwise undefined.
 */
var extractUrlFromIconJsonLd = exports.extractUrlFromIconJsonLd = function extractUrlFromIconJsonLd(icon) {
  if (typeof icon === 'string') {
    return icon;
  } else if (icon['@type'] === 'Link') {
    return (0, _extractUrlFromLinkJsonLd.extractUrlFromLinkJsonLd)(icon);
  } else {
    if (icon.url) {
      return (0, _extractUrlFromLinkJsonLd.extractUrlFromLinkJsonLd)(icon.url);
    }
  }
};