"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.extractPersonFromJsonLd = void 0;
var _url = require("../url");
var extractPersonFromJsonLd = exports.extractPersonFromJsonLd = function extractPersonFromJsonLd(person) {
  if (typeof person === 'string') {
    throw Error('Link.person needs to be an object.');
  } else if (person['@type'] === 'Link') {
    if (person.name) {
      return {
        name: person.name
      };
    }
  } else {
    if (person.name) {
      return {
        name: person.name,
        src: person.icon && (0, _url.extractUrlFromIconJsonLd)(person.icon)
      };
    }
  }
};