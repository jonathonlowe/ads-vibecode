"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.extractContext = void 0;
var _primitives = require("../primitives");
var _url = require("../url");
var extractContext = exports.extractContext = function extractContext(jsonLd) {
  var context = jsonLd.context;
  if (context) {
    if (typeof context === 'string') {
      return {
        name: context
      };
    } else if (context['@type'] === 'Link') {
      if (context.name) {
        return {
          name: context.name
        };
      }
    } else {
      if (context.name) {
        return {
          name: context.name,
          icon: context.icon && (0, _url.extractUrlFromIconJsonLd)(context.icon),
          type: (0, _primitives.extractType)(context)
        };
      }
    }
  }
};