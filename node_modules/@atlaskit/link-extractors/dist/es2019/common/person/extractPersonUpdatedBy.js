import { extractPersonFromJsonLd } from './index';
export const extractPersonUpdatedBy = jsonLd => {
  const updatedBy = jsonLd['atlassian:updatedBy'];
  if (updatedBy) {
    return extractPersonFromJsonLd(updatedBy);
  }
};