import _toConsumableArray from "@babel/runtime/helpers/toConsumableArray";
export var padArray = function padArray(arr, size, value) {
  if (size === 0) {
    return arr;
  }
  var gap = new Array(size);
  gap.fill(value);
  return arr.length > 0 ? [].concat(_toConsumableArray(arr), gap) : gap;
};