import { Token, TokenStream } from 'antlr4ts';
import { Position } from '../common/types';
export declare const getMatchedText: (tokenStream: TokenStream, startTokenIndex: number, stopToken: Token, caretSelectionRange: Position) => string;
