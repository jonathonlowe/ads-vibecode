import { createPlugin } from './pm-plugins/main';
export const submitEditorPlugin = ({
  config: onSave,
  api
}) => ({
  name: 'submitEditor',
  pmPlugins() {
    return [{
      name: 'submitEditor',
      plugin: ({
        dispatch
      }) => createPlugin(dispatch, api, onSave)
    }];
  }
});