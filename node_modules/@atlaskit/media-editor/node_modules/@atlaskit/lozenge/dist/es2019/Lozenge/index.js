import React, { PureComponent } from 'react';
import { Theme } from '../theme';
import Container from './Container';
import Content from './Content';
export default class Lozenge extends PureComponent {
    render() {
        const { theme, children, testId } = this.props;
        return (React.createElement(Theme.Provider, { value: theme },
            React.createElement(Theme.Consumer, Object.assign({}, this.props), themeTokens => (React.createElement(Container, Object.assign({ testId: testId }, themeTokens),
                React.createElement(Content, Object.assign({}, themeTokens), children))))));
    }
}
Lozenge.defaultProps = {
    isBold: false,
    appearance: 'default',
    maxWidth: 200,
};
//# sourceMappingURL=index.js.map