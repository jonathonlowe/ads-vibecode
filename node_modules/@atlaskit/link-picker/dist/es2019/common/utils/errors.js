export class UnauthenticatedError extends Error {
  constructor(iconUrl, authUrl, description) {
    super('The user is not authenticated');
    this.iconUrl = iconUrl;
    this.authUrl = authUrl;
    this.description = description;
  }
}