import { inlines } from './inlines';
export const caption = (node, {
  context
} = {}) => {
  let result = '';
  node.forEach(n => {
    result += inlines(n, {
      context
    });
  });
  return result;
};