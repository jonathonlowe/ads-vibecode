import { listItem } from './listItem';
export const bulletList = (node, {
  context
} = {}) => {
  const result = [];
  node.forEach(item => {
    result.push(listItem(item, '*', context));
  });
  return result.join('\n');
};