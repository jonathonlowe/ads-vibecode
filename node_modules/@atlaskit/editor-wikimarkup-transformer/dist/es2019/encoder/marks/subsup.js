import { baseMarkPattern } from './__base';
export const subsup = (text, attrs) => {
  if (attrs.type === 'sub') {
    return baseMarkPattern(text, '~');
  } else {
    return baseMarkPattern(text, '^');
  }
};