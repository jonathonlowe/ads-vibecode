"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.taskList = void 0;
var _taskItem = require("./taskItem");
var _nestedNode = function nestedNode(node) {
  var depth = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : 1;
  var result = [];
  node.forEach(function (item) {
    if (item.type.name === 'taskList') {
      result.push(_nestedNode(item, depth + 1));
    } else {
      result.push((0, _taskItem.taskItem)(item, depth));
    }
  });
  return result.join('\n');
};
var taskList = exports.taskList = function taskList(node) {
  return _nestedNode(node);
};