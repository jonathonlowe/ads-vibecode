"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.uploadinator = void 0;
var _utils = require("./utils");
var uploadinator = exports.uploadinator = function uploadinator(blobs$, options) {
  var upload = function upload(blob) {
    return options.uploader(blob).then(function () {
      return blob;
    });
  };
  return blobs$.pipe((0, _utils.asyncMap)(upload, options.concurrency));
};