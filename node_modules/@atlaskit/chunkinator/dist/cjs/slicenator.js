"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.slicenator = void 0;
var _range = require("rxjs/observable/range");
var _map = require("rxjs/operators/map");
var slicenator = exports.slicenator = function slicenator(blob, _ref) {
  var chunkSize = _ref.size;
  var totalChunks = Math.ceil(blob.size / chunkSize);
  return (0, _range.range)(0, totalChunks).pipe((0, _map.map)(function (index) {
    return {
      partNumber: index + 1,
      blob: blob.slice(index * chunkSize, (index + 1) * chunkSize)
    };
  }));
};