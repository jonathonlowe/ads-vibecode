import { asyncMap } from './utils';
const uploadinator = (blobs$, options) => {
  const upload = blob => {
    return options.uploader(blob).then(() => blob);
  };
  return blobs$.pipe(asyncMap(upload, options.concurrency));
};
export { uploadinator };