import { createHash } from 'rusha';
import { LRUMap } from 'lru_map';
var URL_HASH_CACHE_SIZE = 100;
var sha1Hash = function sha1Hash(str) {
  return createHash().update(str).digest('hex');
};
var urlCache = new LRUMap(URL_HASH_CACHE_SIZE);
export var getUrlHash = function getUrlHash(url) {
  if (!urlCache.has(url)) {
    urlCache.set(url, sha1Hash(url));
  }
  return urlCache.get(url);
};