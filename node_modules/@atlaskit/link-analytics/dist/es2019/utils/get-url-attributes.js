import { getUrlHash } from './get-url-hash';

/**
 * Returns a set of analytics attributes that can be determined by the URL alone
 */
export const getUrlAttributes = url => {
  var _getUrlHash;
  return {
    urlHash: (_getUrlHash = getUrlHash(url)) !== null && _getUrlHash !== void 0 ? _getUrlHash : 'unknown'
  };
};