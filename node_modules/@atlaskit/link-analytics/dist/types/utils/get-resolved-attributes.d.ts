import { type JsonLd } from '@atlaskit/json-ld-types';
import { type CardType, type CardState } from '@atlaskit/linking-common';
import { type ResolvedAttributesType } from '../common/utils/analytics/analytics.types';
import { type LinkDetails } from '../types';
/**
 * Returns a set of analytics attributes that be
 * derived from a JSON.LD response
 */
export declare const getResolvedAttributes: (linkDetails: LinkDetails, details?: JsonLd.Response, linkStatus?: CardType, error?: CardState['error']) => ResolvedAttributesType;
