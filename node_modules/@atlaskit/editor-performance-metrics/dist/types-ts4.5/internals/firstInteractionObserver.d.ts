declare enum AbortEvent {
    wheel = "wheel",
    keydown = "keydown",
    mousedown = "mousedown",
    pointerdown = "pointerdown",
    pointer = "pointerup",
    touchend = "touchend",
    resize = "resize",
    unknown = "unknown",
    scroll = "scroll"
}
export type OnFirstInteraction = (props: {
    event: keyof typeof AbortEvent;
    time: DOMHighResTimeStamp;
}) => void;
export declare class FirstInteractionObserver {
    private controller;
    private unbindCallbacks;
    constructor(onFirstInteraction: OnFirstInteraction);
    private unbindFirstInteractionEvents;
    disconnect(): void;
}
export {};
