import { EditorPerformanceObserver } from './editorPerformanceObserver';
import { TimelineController } from './timeline';
export const getGlobalEditorMetricsObserver = options => {
  let observer = globalThis.__editor_performance_metrics_observer;
  if (observer) {
    return observer;
  }
  const timeline = new TimelineController();
  observer = new EditorPerformanceObserver(timeline, options);
  globalThis.__editor_performance_metrics_timeline = timeline;
  globalThis.__editor_performance_metrics_observer = observer;
  return observer;
};