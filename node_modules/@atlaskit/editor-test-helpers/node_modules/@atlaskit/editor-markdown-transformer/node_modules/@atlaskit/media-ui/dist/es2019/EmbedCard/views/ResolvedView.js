/** @jsx jsx */
import { jsx } from '@emotion/core';
import LinkGlyph from '@atlaskit/icon/glyph/link';
import { ExpandedFrame } from '../components/ExpandedFrame';
import { ImageIcon } from '../components/ImageIcon';
import { Frame } from '../components/Frame';
export const EmbedCardResolvedView = ({ link, context, onClick, isSelected, isFrameVisible, preview, title, testId = 'embed-card-resolved-view', inheritDimensions, }) => {
    const src = context && context.icon
        ? typeof context.icon === 'string'
            ? context.icon
            : undefined
        : undefined;
    const text = title || (context && context.text);
    return (jsx(ExpandedFrame, { isSelected: isSelected, isFrameVisible: isFrameVisible, href: link, testId: testId, icon: jsx(ImageIcon, { src: src, default: jsx(LinkGlyph, { label: "icon", size: "small" }) }), text: text, onClick: onClick, inheritDimensions: inheritDimensions },
        jsx(Frame, { url: preview, testId: testId })));
};
//# sourceMappingURL=ResolvedView.js.map