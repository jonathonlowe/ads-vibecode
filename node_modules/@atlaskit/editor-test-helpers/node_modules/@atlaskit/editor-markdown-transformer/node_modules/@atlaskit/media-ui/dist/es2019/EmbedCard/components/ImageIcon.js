import React from 'react';
import ImageLoader from 'react-render-image';
import { Image } from './styled';
export const ImageIcon = ({ alt = '', src, size = 16, title, default: defaultIcon, }) => {
    // TODO: do we need this?
    if (!src) {
        return defaultIcon || null;
    }
    return (React.createElement(ImageLoader, { src: src, loading: defaultIcon, loaded: React.createElement(Image, { className: "smart-link-icon", src: src, alt: alt, size: size, title: title }), errored: defaultIcon }));
};
//# sourceMappingURL=ImageIcon.js.map