import { Signal } from '../signal';
export class DefaultDrawingArea {
    constructor(canvas, size, backgroundColor) {
        this.canvas = canvas;
        this.size = size;
        this.backgroundColor = backgroundColor;
        this.resize = new Signal();
        this.setCanvasSize();
    }
    get outputSize() {
        return this.size;
    }
    unload() { }
    setSize(size) {
        this.size = size;
        this.setCanvasSize();
        this.resize.emit(size);
    }
    setCanvasSize() {
        this.canvas.width = this.size.width;
        this.canvas.height = this.size.height;
    }
}
//# sourceMappingURL=drawingArea.js.map