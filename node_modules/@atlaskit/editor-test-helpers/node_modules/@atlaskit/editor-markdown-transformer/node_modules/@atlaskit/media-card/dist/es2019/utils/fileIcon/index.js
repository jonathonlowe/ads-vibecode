import React from 'react';
import { Component } from 'react';
import { MediaTypeIcon } from '../mediaTypeIcon';
import { FileTypeIcon } from './styled';
const fileTypeIconClass = 'file-type-icon';
export class FileIcon extends Component {
    render() {
        const { mediaType, iconUrl, style } = this.props;
        const type = mediaType || 'unknown';
        const defaultIcon = (React.createElement(MediaTypeIcon, { type: mediaType, size: "small", className: fileTypeIconClass }));
        const icon = iconUrl ? (React.createElement("img", { src: iconUrl, className: "custom-icon", alt: type })) : (defaultIcon);
        return (React.createElement(FileTypeIcon, { style: style, className: fileTypeIconClass }, icon));
    }
}
//# sourceMappingURL=index.js.map