import styled from 'styled-components';
import { gridSize, colors, themed } from '@atlaskit/theme';
export const EditorIconWrapper = styled.span `
  flex: 0 0 16px;
  height: 16px;
  width: 16px;
  color: ${(props) => props.showPlaceholder
    ? colors.N100
    : themed({ light: colors.G300, dark: colors.G200 })};
  margin: 4px ${gridSize() * 1.5}px 0 0;

  > span {
    margin: -8px;
  }
`;
//# sourceMappingURL=DecisionItem.js.map