import { ExtensionKey, ExtensionManifest, ExtensionProvider, ExtensionType } from './types';
export default class DefaultExtensionProvider implements ExtensionProvider {
    private manifestsPromise;
    constructor(manifests: ExtensionManifest[] | Promise<ExtensionManifest[]>);
    getExtensions(): Promise<ExtensionManifest[]>;
    getExtension(type: ExtensionType, key: ExtensionKey): Promise<ExtensionManifest>;
    search(keyword: string): Promise<ExtensionManifest[]>;
}
