import React from 'react';
import { Frame } from '../Frame';
import Lozenge from '@atlaskit/lozenge';
import { IconAndTitleLayout } from '../IconAndTitleLayout';
import { LozengeWrapper } from '../IconAndTitleLayout/styled';
export class InlineCardResolvedView extends React.Component {
    renderLozenge() {
        const { lozenge } = this.props;
        if (!lozenge) {
            return null;
        }
        return (React.createElement(LozengeWrapper, null,
            React.createElement(Lozenge, { appearance: lozenge.appearance || 'default', isBold: lozenge.isBold }, lozenge.text)));
    }
    render() {
        const { title = '', isSelected, onClick, icon, link, testId = 'inline-card-resolved-view', titleTextColor, } = this.props;
        return (React.createElement(Frame, { testId: testId, link: link, isSelected: isSelected, onClick: onClick },
            React.createElement(IconAndTitleLayout, { icon: icon, title: title, titleTextColor: titleTextColor }),
            this.renderLozenge()));
    }
}
//# sourceMappingURL=index.js.map