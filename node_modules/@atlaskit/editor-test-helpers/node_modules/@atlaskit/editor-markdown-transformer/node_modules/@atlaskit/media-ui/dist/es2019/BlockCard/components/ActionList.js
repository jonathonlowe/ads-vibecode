/** @jsx jsx */
import { jsx } from '@emotion/core';
import { useState } from 'react';
import Item from '@atlaskit/item';
import DropList from '@atlaskit/droplist';
import Button, { ButtonGroup } from '@atlaskit/button';
import { Action } from './Action';
import { gs, mq } from '../utils';
export const ActionList = ({ items }) => {
    const [isOpen, setOpen] = useState(false);
    const actionsToShow = items.slice(0, 2);
    const actionsToList = items.slice(2, items.length);
    return (jsx("div", { css: mq({ display: 'flex', marginTop: [gs(2), 0] }) },
        jsx(ButtonGroup, null, actionsToShow.map(action => (jsx(Action, Object.assign({ key: action.id }, action))))),
        actionsToList.length ? (jsx("div", { css: { marginLeft: gs(0.5) } },
            jsx(DropList, { appearance: "default", position: "right top", isTriggerNotTabbable: true, onOpenChange: () => setOpen(true), onClick: () => setOpen(!isOpen), isOpen: isOpen, trigger: jsx(Button, { spacing: "compact" }, "...") }, actionsToList.map(actionToList => (jsx(Item, { key: actionToList.id }, actionToList.text)))))) : null));
};
//# sourceMappingURL=ActionList.js.map