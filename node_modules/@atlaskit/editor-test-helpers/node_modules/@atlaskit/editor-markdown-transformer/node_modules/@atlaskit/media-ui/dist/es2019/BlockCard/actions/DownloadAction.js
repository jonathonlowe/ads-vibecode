import React from 'react';
import { FormattedMessage } from 'react-intl';
import { downloadUrl } from '@atlaskit/media-common';
import { messages } from '../../messages';
export async function downloadFunction({ url }) {
    if (!url) {
        return;
    }
    downloadUrl(url);
}
export const DownloadAction = ({ url }) => ({
    id: 'download-content',
    text: React.createElement(FormattedMessage, Object.assign({}, messages.download)),
    promise: () => downloadFunction({
        url,
    }),
});
//# sourceMappingURL=DownloadAction.js.map