import React from 'react';
import { Component } from 'react';
import { CardActionButton } from './styled';
export class CardActionIconButton extends Component {
    constructor() {
        super(...arguments);
        // this is to prevent currently focused text to loose cursor on clicking card action
        // this does not prevent onclick behavior
        this.onMouseDown = (event) => {
            event.preventDefault();
        };
    }
    render() {
        const { icon, triggerColor, onClick } = this.props;
        return (React.createElement(CardActionButton, { "data-testid": "media-card-primary-action", onClick: onClick, onMouseDown: this.onMouseDown, style: { color: triggerColor } }, icon));
    }
}
//# sourceMappingURL=cardActionIconButton.js.map