import React from 'react';
import { Component } from 'react';
import FileIcon from '@atlaskit/icon/glyph/file';
import { getDimensionsWithDefault } from './getDimensionsWithDefault';
import { Wrapper } from './styled';
export class CardLoading extends Component {
    render() {
        const { dimensions: dimensionsProp, testId } = this.props;
        const dimensions = getDimensionsWithDefault(dimensionsProp);
        return (React.createElement(Wrapper, { "data-testid": testId || 'media-card-loading', "data-test-loading": true, dimensions: dimensions }, this.icon));
    }
    get icon() {
        return React.createElement(FileIcon, { label: "loading", size: "medium" });
    }
}
//# sourceMappingURL=cardLoading.js.map