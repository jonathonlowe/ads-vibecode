import React from 'react';
import cx from 'classnames';
import { Component } from 'react';
import { A } from './styled';
export class Href extends Component {
    render() {
        const { linkUrl, underline, children, className, ...otherProps } = this.props;
        const classNames = cx(className, { underline });
        return (React.createElement(A, Object.assign({}, otherProps, { href: linkUrl, className: classNames, target: "_blank", rel: "noopener" }), children));
    }
}
//# sourceMappingURL=index.js.map