export const isDecision = (item) => !!(item && item.type === 'DECISION');
export const isTask = (item) => !!(item && item.type === 'TASK');
export const toObjectKey = (item) => {
    const { localId, objectAri } = item;
    return {
        localId,
        objectAri,
    };
};
export const objectKeyToString = (objectKey) => {
    const { objectAri, localId } = objectKey;
    return `${objectAri}:${localId}`;
};
export const toggleTaskState = (state) => state === 'DONE' ? 'TODO' : 'DONE';
//# sourceMappingURL=type-helpers.js.map