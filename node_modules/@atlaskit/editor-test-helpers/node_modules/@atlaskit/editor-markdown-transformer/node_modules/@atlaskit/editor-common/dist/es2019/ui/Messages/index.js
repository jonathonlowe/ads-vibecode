import React from 'react';
import styled from 'styled-components';
import SuccessIcon from '@atlaskit/icon/glyph/editor/success';
import ErrorIcon from '@atlaskit/icon/glyph/error';
import { G400, N200, R400 } from '@atlaskit/theme/colors';
import { gridSize } from '@atlaskit/theme/constants';
import { multiply } from '@atlaskit/theme/math';
import { h200 } from '@atlaskit/theme/typography';
const Message = styled.div `
  ${h200} font-weight: normal;
  color: ${props => {
    if (props.error) {
        return R400;
    }
    if (props.valid) {
        return G400;
    }
    return N200;
}};
  margin-top: ${multiply(gridSize, 0.5)}px;
  display: flex;
  justify-content: baseline;
`;
const IconWrapper = styled.span `
  display: flex;
  margin-right: 4px;
`;
export const HelperMessage = ({ children }) => (React.createElement(Message, null, children));
export const ErrorMessage = ({ children }) => (React.createElement(Message, { error: true },
    React.createElement(IconWrapper, null,
        React.createElement(ErrorIcon, { size: "small", label: "error", "aria-label": "error" })),
    children));
export const ValidMessage = ({ children }) => (React.createElement(Message, { valid: true },
    React.createElement(IconWrapper, null,
        React.createElement(SuccessIcon, { size: "small", label: "success" })),
    children));
//# sourceMappingURL=index.js.map