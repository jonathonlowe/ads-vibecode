import styled from 'styled-components';
import { N500, N30, N0 } from '@atlaskit/theme/colors';
export const TOTAL_CIRCLE_SIZE = 18;
export const MainArea = styled.div `
  box-sizing: border-box;
  width: ${TOTAL_CIRCLE_SIZE}px;
  height: ${TOTAL_CIRCLE_SIZE}px;
  border-radius: 15px;
  background-color: ${(props) => (props.isActive ? N500 : N30)};
`;
export const FrontArea = styled.div `
  box-sizing: border-box;
  background-color: ${(props) => (props.isActive ? N0 : N500)};
`;
//# sourceMappingURL=lineWidthButtonStyles.js.map