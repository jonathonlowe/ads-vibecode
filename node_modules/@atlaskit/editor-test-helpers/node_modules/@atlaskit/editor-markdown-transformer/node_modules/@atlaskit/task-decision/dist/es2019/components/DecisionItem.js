import React from 'react';
import { PureComponent } from 'react';
import DecisionIcon from '@atlaskit/icon/glyph/editor/decision';
import { EditorIconWrapper } from '../styled/DecisionItem';
import Item from './Item';
export default class DecisionItem extends PureComponent {
    render() {
        const { appearance, children, contentRef, placeholder, showPlaceholder, dataAttributes, } = this.props;
        const icon = (React.createElement(EditorIconWrapper, { showPlaceholder: showPlaceholder },
            React.createElement(DecisionIcon, { label: "Decision", size: "large" })));
        return (React.createElement(Item, { appearance: appearance, contentRef: contentRef, icon: icon, placeholder: placeholder, showPlaceholder: showPlaceholder, itemType: "DECISION", dataAttributes: dataAttributes }, children));
    }
}
DecisionItem.defaultProps = {
    appearance: 'inline',
};
//# sourceMappingURL=DecisionItem.js.map