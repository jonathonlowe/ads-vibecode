/** @jsx jsx */
import { jsx } from '@emotion/core';
import { gs, mq } from '../utils';
export const Content = ({ children }) => (jsx("div", { css: mq({
        padding: gs(2),
        display: 'flex',
        flexDirection: 'column',
        justifyContent: ['unset', 'space-between'],
        flexGrow: 1,
    }) }, children));
//# sourceMappingURL=Content.js.map