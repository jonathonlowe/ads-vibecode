import { jsx } from '@emotion/core';
import { components } from 'react-select';
import { layers } from '@atlaskit/theme/constants';
import { N40A } from '@atlaskit/theme/colors';
import SearchIcon from '@atlaskit/icon/glyph/editor/search';
export const MenuDialog = ({ maxWidth, minWidth, ...props }) => (jsx("div", Object.assign({ css: {
        backgroundColor: 'white',
        borderRadius: 4,
        boxShadow: `0 0 0 1px ${N40A}, 0 4px 11px ${N40A}`,
        maxWidth,
        minWidth,
        zIndex: layers.modal(),
    } }, props)));
// ==============================
// Custom Components
// ==============================
const DropdownIndicator = () => (jsx("div", { css: { marginRight: 2, textAlign: 'center', width: 32 } },
    jsx(SearchIcon, { label: "open" })));
const Control = ({ innerRef, innerProps, ...props }) => (jsx("div", { ref: innerRef, css: { padding: '8px 8px 4px' } },
    jsx(components.Control, Object.assign({}, props, { innerProps: innerProps }))));
export const DummyControl = props => (jsx("div", { css: {
        border: 0,
        clip: 'rect(1px, 1px, 1px, 1px)',
        height: 1,
        overflow: 'hidden',
        padding: 0,
        position: 'absolute',
        whiteSpace: 'nowrap',
        width: 1,
    } },
    jsx(components.Control, Object.assign({}, props))));
// NOTE `props` intentionally omitted from `Fragment`
// eslint-disable-next-line
const Menu = ({ children, innerProps, ...props }) => (jsx("div", Object.assign({}, innerProps), children));
export const defaultComponents = { Control, DropdownIndicator, Menu };
//# sourceMappingURL=components.js.map