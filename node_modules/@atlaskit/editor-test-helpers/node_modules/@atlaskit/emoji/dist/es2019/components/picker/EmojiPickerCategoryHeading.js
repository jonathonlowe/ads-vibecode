import classnames from 'classnames';
import React from 'react';
import { PureComponent } from 'react';
import { FormattedMessage } from 'react-intl';
import { isMessagesKey } from '../../util/type-helpers';
import { messages } from '../i18n';
import * as styles from './styles';
export default class EmojiPickerCategoryHeading extends PureComponent {
    render() {
        const { id, title, className } = this.props;
        return (React.createElement("div", { id: id, "data-category-id": id, className: classnames(className) },
            React.createElement("div", { className: styles.emojiCategoryTitle }, isMessagesKey(title) ? (React.createElement(FormattedMessage, Object.assign({}, messages[title]))) : (title))));
    }
}
//# sourceMappingURL=EmojiPickerCategoryHeading.js.map