import React from 'react';
import classNames from 'classnames';
import Tooltip from '@atlaskit/tooltip';
import { placeholder, placeholderContainer } from './styles';
import { defaultEmojiHeight } from '../../util/constants';
import { isImageRepresentation, isMediaRepresentation, } from '../../util/type-helpers';
const EmojiPlaceholder = (props) => {
    const { shortName, size = defaultEmojiHeight, showTooltip, representation, } = props;
    let scaledWidth;
    let scaledHeight;
    if (representation &&
        size &&
        (isImageRepresentation(representation) ||
            isMediaRepresentation(representation))) {
        const width = representation.width;
        const height = representation.height;
        if (width && height) {
            scaledWidth = (size / height) * width;
            scaledHeight = size;
        }
    }
    const width = scaledWidth || size;
    const height = scaledHeight || size;
    const style = {
        fill: 'f7f7f7',
        width: `${width}px`,
        height: `${height}px`,
    };
    const classes = {
        [placeholder]: true,
        [placeholderContainer]: true,
    };
    const placeholderNode = (React.createElement("span", { "aria-label": shortName, className: classNames(classes), style: style }));
    return showTooltip ? (React.createElement(Tooltip, { tag: "span", content: shortName }, placeholderNode)) : (placeholderNode);
};
export default EmojiPlaceholder;
//# sourceMappingURL=EmojiPlaceholder.js.map