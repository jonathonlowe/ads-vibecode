import React from 'react';
import { PureComponent } from 'react';
import AkButton from '@atlaskit/button';
export default class FileChooser extends PureComponent {
    constructor() {
        super(...arguments);
        this.onChooseFile = () => {
            if (this.props.onClick) {
                this.props.onClick();
            }
            const chooseFile = this.refs['chooseFile'];
            if (chooseFile) {
                chooseFile.click();
            }
        };
    }
    render() {
        const { accept, ariaLabel, isDisabled, label, onChange } = this.props;
        return (React.createElement("span", null,
            React.createElement(AkButton, { onClick: this.onChooseFile, isDisabled: isDisabled, "aria-label": ariaLabel || label }, label),
            React.createElement("input", { className: "emojiUploadFileInput", ref: "chooseFile", onChange: onChange, type: "file", accept: accept, style: { display: 'none' } })));
    }
}
//# sourceMappingURL=FileChooser.js.map