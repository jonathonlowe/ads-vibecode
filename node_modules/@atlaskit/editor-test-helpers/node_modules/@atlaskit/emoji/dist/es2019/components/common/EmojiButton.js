import classNames from 'classnames';
import React from 'react';
import * as styles from './styles';
import Emoji from './Emoji';
import { leftClick } from '../../util/mouse';
const handleMouseDown = (props, event) => {
    const { onSelected } = props;
    event.preventDefault();
    if (onSelected && leftClick(event)) {
        onSelected();
    }
};
export const EmojiButton = (props) => {
    const { emoji, selectOnHover } = props;
    const classes = [styles.emojiButton];
    return (React.createElement("button", { className: classNames(classes), onMouseDown: event => {
            handleMouseDown(props, event);
        } },
        React.createElement(Emoji, { emoji: emoji, selectOnHover: selectOnHover })));
};
export default EmojiButton;
//# sourceMappingURL=EmojiButton.js.map