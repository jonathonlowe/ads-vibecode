import { ReplaySubject } from 'rxjs/ReplaySubject';
export const createFileStateSubject = (initialState) => {
    const subject = new ReplaySubject(1);
    if (initialState) {
        subject.next(initialState);
    }
    return subject;
};
//# sourceMappingURL=createFileStateSubject.js.map