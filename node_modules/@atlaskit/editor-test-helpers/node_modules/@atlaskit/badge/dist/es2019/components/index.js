import React from 'react';
import GlobalTheme from '@atlaskit/theme/components';
import { Theme } from '../theme';
import Container from './Container';
import Format from './Format';
const Badge = ({ theme, appearance = 'default', children = 0, max = 99, testId, }) => {
    return (React.createElement(Theme.Provider, { value: theme },
        React.createElement(GlobalTheme.Consumer, null, ({ mode }) => (React.createElement(Theme.Consumer, { appearance: appearance, mode: mode }, (tokens) => (React.createElement(Container, Object.assign({}, tokens, { "data-testid": testId }), typeof children === 'string' ? (children) : (React.createElement(Format, { max: max }, children)))))))));
};
export default Badge;
//# sourceMappingURL=index.js.map