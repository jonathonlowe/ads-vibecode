export const padArray = (arr, size, value) => {
    if (size === 0) {
        return arr;
    }
    const gap = new Array(size);
    gap.fill(value);
    return arr.length > 0 ? [...arr, ...gap] : gap;
};
//# sourceMappingURL=index.js.map