import { StatelessComponent } from 'react';
import { Props } from './helper/createNamespaceContext';
export declare const MEDIA_CONTEXT = "mediaCtx";
interface AnalyticsFileAttributes {
    fileSource: string;
    fileMediatype?: 'doc' | 'audio' | 'video' | 'image' | 'unknown';
    fileMimetype?: string;
    fileId?: string;
    fileStatus?: 'error' | 'failed-processing' | 'processed' | 'processing' | 'uploading';
    fileSize?: number;
}
export declare type MediaAnalyticsData = {
    fileAttributes?: AnalyticsFileAttributes;
} & Props['data'];
declare type MediaAnalyticsContextProps = Props & {
    data: MediaAnalyticsData;
};
export declare const MediaAnalyticsContext: StatelessComponent<MediaAnalyticsContextProps>;
export {};
