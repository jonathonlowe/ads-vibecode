import { sizeGap } from '../../util/constants';
export const emojiPickerHeightOffset = size => {
  if (size === 'medium') {
    return sizeGap;
  } else if (size === 'large') {
    return sizeGap * 2;
  }
  return 0;
};

// this function is being exported so it can be mocked in unit tests
export const scrollToRow = (listRef, index) => {
  var _listRef$current;
  (_listRef$current = listRef.current) === null || _listRef$current === void 0 ? void 0 : _listRef$current.scrollToRow(index);
};