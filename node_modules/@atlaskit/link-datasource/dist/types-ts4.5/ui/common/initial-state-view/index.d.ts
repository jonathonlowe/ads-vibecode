/// <reference types="react" />
import { type MessageDescriptor } from 'react-intl-next';
interface InitialStateViewProps {
    icon: JSX.Element;
    showBeta?: boolean;
    title: MessageDescriptor;
    description: MessageDescriptor;
    learnMoreLink?: {
        href: string;
        text: MessageDescriptor;
    };
}
export declare const InitialStateView: ({ icon, showBeta, title, description, learnMoreLink, }: InitialStateViewProps) => JSX.Element;
export {};
