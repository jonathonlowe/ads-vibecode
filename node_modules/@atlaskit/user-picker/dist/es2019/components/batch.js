export function batchByKey(callback) {
  const calls = {};
  return (key, ...args) => {
    if (!calls[key]) {
      window.setTimeout(() => {
        callback(key, calls[key]);
        delete calls[key];
      });
      calls[key] = [];
    }
    calls[key].push(args);
  };
}