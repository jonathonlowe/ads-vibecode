import { userIntentPluginKey } from '../pm-plugins/main';
export const setCurrentUserIntent = currentUserIntent => ({
  tr
}) => {
  tr.setMeta(userIntentPluginKey, {
    type: 'setCurrentUserIntent',
    data: {
      currentUserIntent
    }
  });
  return tr;
};