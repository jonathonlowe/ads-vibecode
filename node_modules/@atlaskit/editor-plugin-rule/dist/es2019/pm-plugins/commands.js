import { createHorizontalRule } from './input-rule';
export const insertHorizontalRule = editorAnalyticsAPI => inputMethod => (state, dispatch) => {
  const tr = createHorizontalRule(state, state.selection.from, state.selection.to, inputMethod, editorAnalyticsAPI);
  if (tr) {
    if (dispatch) {
      dispatch(tr);
    }
    return true;
  }
  return false;
};