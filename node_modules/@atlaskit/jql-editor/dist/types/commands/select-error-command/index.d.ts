import { type JQLEditorCommand } from '../../schema';
/**
 * A Prosemirror Command that will select the first JQL error in the query if present.
 */
export declare const selectErrorCommand: JQLEditorCommand;
