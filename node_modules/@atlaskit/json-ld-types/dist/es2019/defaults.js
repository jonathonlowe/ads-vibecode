const defaultMetaPublic = {
  visibility: 'public',
  access: 'granted'
};
const defaultMetaGranted = {
  visibility: 'restricted',
  access: 'granted'
};
const defaultMetaUnauthorized = {
  visibility: 'restricted',
  access: 'unauthorized'
};
const defaultMetaPermissionDenied = {
  visibility: 'restricted',
  access: 'forbidden'
};
const defaultMetaNotFound = {
  visibility: 'not_found',
  access: 'forbidden'
};
export const defaults = {
  meta: {
    public: defaultMetaPublic,
    granted: defaultMetaGranted,
    unauthorized: defaultMetaUnauthorized,
    permissionDenied: defaultMetaPermissionDenied,
    notFound: defaultMetaNotFound
  }
};