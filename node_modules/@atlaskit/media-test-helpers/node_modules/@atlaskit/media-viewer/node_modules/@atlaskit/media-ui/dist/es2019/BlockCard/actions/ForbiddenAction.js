import React from 'react';
import { FormattedMessage } from 'react-intl';
import { messages } from '../../messages';
export const ForbiddenAction = (handler) => ({
    id: 'connect-other-account',
    text: React.createElement(FormattedMessage, Object.assign({}, messages.try_another_account)),
    promise: () => new Promise(resolve => resolve(handler())),
    buttonAppearance: 'primary',
});
//# sourceMappingURL=ForbiddenAction.js.map