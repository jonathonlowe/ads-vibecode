import { MetadataProps } from './Metadata';
export interface MetadataListProps {
    items: MetadataProps[];
    testId?: string;
}
export declare const MetadataList: ({ items, testId }: MetadataListProps) => JSX.Element;
