/** @jsx jsx */
import { jsx } from '@emotion/core';
import AvatarGroup from '@atlaskit/avatar-group';
import { mq } from '../utils';
export const CollaboratorList = ({ items, handleAvatarClick = () => { }, handleMoreAvatarsClick = () => { }, }) => {
    if (items.length === 0) {
        return null;
    }
    return (jsx("span", { css: mq({ display: ['none', 'inherit'] }) },
        jsx(AvatarGroup, { maxCount: 4, appearance: "stack", size: "small", data: items, onAvatarClick: event => {
                event.stopPropagation();
                event.preventDefault();
                handleAvatarClick(event);
            }, onMoreClick: event => {
                event.stopPropagation();
                event.preventDefault();
                handleMoreAvatarsClick(event);
            } })));
};
//# sourceMappingURL=CollaboratorList.js.map