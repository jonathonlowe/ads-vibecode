/** @jsx jsx */
import { jsx } from '@emotion/core';
import { N300 } from '@atlaskit/theme/colors';
import { gs } from '../utils';
export var Provider = function (_a) {
    var name = _a.name, icon = _a.icon;
    var iconToRender = icon || null;
    if (typeof icon === 'string') {
        iconToRender = (jsx("img", { className: "smart-link-icon", css: { height: gs(1.5), width: gs(1.5) }, src: icon }));
    }
    return (jsx("div", { css: { display: 'flex', alignItems: 'center' } },
        iconToRender,
        jsx("span", { css: {
                fontSize: gs(1.5),
                color: N300,
                margin: 0,
                marginLeft: gs(0.5),
                // EDM-713: fixes copy-paste from renderer to editor for Firefox
                // due to HTML its unwrapping behaviour on paste.
                MozUserSelect: 'none',
            } }, name)));
};
//# sourceMappingURL=Provider.js.map