export async function viewFunction({ url }) {
    if (!url) {
        return;
    }
    window.open(url, '_blank', 'noopener=yes');
}
export const ViewAction = ({ url }) => ({
    id: 'view-content',
    text: 'View',
    promise: () => viewFunction({
        url,
    }),
});
//# sourceMappingURL=ViewAction.js.map