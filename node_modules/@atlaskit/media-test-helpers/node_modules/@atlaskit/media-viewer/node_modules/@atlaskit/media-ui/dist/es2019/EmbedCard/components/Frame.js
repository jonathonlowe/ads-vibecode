/** @jsx jsx */
import { jsx } from '@emotion/core';
export const Frame = ({ url, testId }) => {
    if (!url) {
        return null;
    }
    return (jsx("iframe", { src: url, "data-testid": `${testId}-frame`, css: {
            border: 0,
            top: 0,
            left: 0,
            width: '100%',
            height: '100%',
            position: 'relative',
            overflow: 'hidden',
            borderRadius: '3px',
        }, allowFullScreen: true, scrolling: "yes", allow: "autoplay; encrypted-media" }));
};
//# sourceMappingURL=Frame.js.map