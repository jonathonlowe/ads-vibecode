import { __assign } from "tslib";
/** @jsx jsx */
import { jsx } from '@emotion/core';
import DocumentFilledIcon from '@atlaskit/icon/glyph/document-filled';
import { N50, N90 } from '@atlaskit/theme/colors';
import { fontSize } from '@atlaskit/theme/constants';
import { Frame } from '../components/Frame';
import { gs } from '../utils';
import { FormattedMessage } from 'react-intl';
import { messages } from '../../messages';
export var ResolvingView = function (_a) {
    var _b = _a.isSelected, isSelected = _b === void 0 ? false : _b, _c = _a.testId, testId = _c === void 0 ? 'block-card-resolving-view' : _c, inheritDimensions = _a.inheritDimensions;
    return (jsx(Frame, { inheritDimensions: inheritDimensions, compact: true, isSelected: isSelected, testId: testId },
        jsx(DocumentFilledIcon, { size: "small", primaryColor: N50, label: "document-icon" }),
        jsx("span", { css: {
                fontSize: fontSize() + "px",
                color: N90,
                marginLeft: gs(0.5),
            } },
            jsx(FormattedMessage, __assign({}, messages.loading)))));
};
//# sourceMappingURL=ResolvingView.js.map