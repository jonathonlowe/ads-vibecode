import React, { Component } from 'react';
import { Inner, Outer } from '../styled/MoreIndicator';
import { getProps, withPseudoState } from '../utils';
const MAX_DISPLAY_COUNT = 99;
class MoreIndicator extends Component {
    render() {
        const outerProps = getProps(this);
        const { appearance, isActive, isFocus, isHover, size, count, testId, } = this.props;
        const displayCount = count > MAX_DISPLAY_COUNT ? MAX_DISPLAY_COUNT : count;
        return (React.createElement(Outer, Object.assign({}, outerProps, { isInteractive: true, "data-testid": testId }),
            React.createElement(Inner, { appearance: appearance, isActive: isActive, isFocus: isFocus, isHover: isHover, size: size },
                "+",
                displayCount)));
    }
}
MoreIndicator.defaultProps = {
    count: 0,
    appearance: 'circle',
    size: 'medium',
};
export default withPseudoState(MoreIndicator);
//# sourceMappingURL=MoreIndicator.js.map