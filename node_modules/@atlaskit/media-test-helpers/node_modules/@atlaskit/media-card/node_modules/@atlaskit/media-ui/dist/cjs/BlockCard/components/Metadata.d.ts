/// <reference types="react" />
export interface MetadataProps {
    text: string;
    icon?: React.ReactNode;
    iconUrl?: string;
    tooltip?: string;
}
export declare const Metadata: ({ text, icon, iconUrl, tooltip }: MetadataProps) => JSX.Element;
