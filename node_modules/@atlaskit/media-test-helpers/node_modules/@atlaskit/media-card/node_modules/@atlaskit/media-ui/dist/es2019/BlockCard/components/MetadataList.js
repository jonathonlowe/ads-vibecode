/** @jsx jsx */
import { jsx } from '@emotion/core';
import { Metadata } from './Metadata';
import { gs } from '../utils';
export const MetadataList = ({ items, testId }) => {
    return (jsx("div", { css: {
            display: 'flex',
            marginTop: gs(0.5),
            alignItems: 'center',
            '&:last-child': {
                marginRight: '0px',
            },
        }, "data-testid": testId }, items.map(item => (jsx(Metadata, Object.assign({ key: item.text }, item))))));
};
//# sourceMappingURL=MetadataList.js.map