/// <reference types="react" />
import { ActionProps } from '../components/Action';
import { IconProps } from '../components/Icon';
export interface UnauthorizedViewProps {
    actions: ActionProps[];
    context?: {
        icon?: React.ReactNode;
        text: string;
    };
    isSelected?: boolean;
    testId?: string;
    showActions?: boolean;
    icon: IconProps;
    link?: string;
    onClick?: React.EventHandler<React.MouseEvent | React.KeyboardEvent>;
}
export declare const UnauthorizedView: ({ context, isSelected, actions, testId, showActions, icon, link, onClick, }: UnauthorizedViewProps) => JSX.Element;
