/// <reference types="react" />
import { AvatarClickType } from '@atlaskit/avatar';
import { Collaborator } from '../components/CollaboratorList';
import { IconProps } from '../components/Icon';
import { ActionProps } from '../components/Action';
import { MetadataProps } from '../components/Metadata';
import { LozengeProps } from '../../common';
import { ContextViewModel } from '../../types';
export interface ResolvedViewProps {
    context?: ContextViewModel;
    link?: string;
    icon: IconProps;
    details?: Array<MetadataProps>;
    byline?: React.ReactNode;
    lozenge?: LozengeProps;
    thumbnail?: string;
    title?: string;
    users?: Collaborator[];
    actions?: Array<ActionProps>;
    handleAvatarClick?: AvatarClickType;
    handleMoreAvatarsClick?: React.MouseEventHandler;
    onClick?: React.EventHandler<React.MouseEvent | React.KeyboardEvent>;
    isSelected?: boolean;
    testId?: string;
    showActions?: boolean;
}
export declare const ResolvedView: ({ icon, actions, thumbnail, context, title, isSelected, users, handleAvatarClick, handleMoreAvatarsClick, onClick, link, byline, lozenge, details, testId, showActions, }: ResolvedViewProps) => JSX.Element;
