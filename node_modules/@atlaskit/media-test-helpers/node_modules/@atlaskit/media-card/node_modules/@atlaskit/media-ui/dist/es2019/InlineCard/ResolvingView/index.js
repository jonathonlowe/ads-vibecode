import React from 'react';
import { Frame } from '../Frame';
import Spinner from '@atlaskit/spinner';
import { IconAndTitleLayout } from '../IconAndTitleLayout';
import { SpinnerWrapper } from './styled';
export class InlineCardResolvingView extends React.Component {
    render() {
        const { url, onClick, isSelected, testId } = this.props;
        return (React.createElement(Frame, { testId: testId, onClick: onClick, isSelected: isSelected },
            React.createElement(IconAndTitleLayout, { title: url },
                React.createElement(SpinnerWrapper, { className: "inline-resolving-spinner" },
                    React.createElement(Spinner, { size: 14 })))));
    }
}
//# sourceMappingURL=index.js.map