import React from 'react';
export interface FrameProps {
    children?: React.ReactNode;
    compact?: boolean;
    isSelected?: boolean;
    isHoverable?: boolean;
    testId?: string;
    inheritDimensions?: boolean;
}
export declare const Frame: (props?: FrameProps) => JSX.Element;
export declare const ExpandedFrame: ({ children, isSelected, isHoverable, testId, }: FrameProps) => JSX.Element;
export declare const CompactFrame: ({ children, isHoverable, isSelected, testId, inheritDimensions, }: FrameProps) => JSX.Element;
