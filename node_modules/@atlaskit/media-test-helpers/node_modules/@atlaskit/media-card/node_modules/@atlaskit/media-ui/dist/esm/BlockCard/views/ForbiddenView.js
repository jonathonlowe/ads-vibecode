import { __assign } from "tslib";
/** @jsx jsx */
import { jsx } from '@emotion/core';
import { FormattedMessage } from 'react-intl';
import { R50 } from '@atlaskit/theme/colors';
import { Frame } from '../components/Frame';
import { Thumbnail } from '../components/Thumbnail';
import { Provider } from '../components/Provider';
import { Name } from '../components/Name';
import { Byline } from '../components/Byline';
import { ActionList } from '../components/ActionList';
import { Content } from '../components/Content';
import { LockImage } from '../utils/constants';
import { messages } from '../../messages';
import { ContentFooter } from '../components/ContentFooter';
import { Icon } from '../components/Icon';
import { ContentHeader } from '../components/ContentHeader';
import { handleClickCommon } from '../utils/handlers';
var textTitleProps = __assign({}, messages.invalid_permissions);
var textDescriptionProps = __assign({}, messages.invalid_permissions_description);
export var ForbiddenView = function (_a) {
    var _b = _a.context, context = _b === void 0 ? { text: '' } : _b, _c = _a.isSelected, isSelected = _c === void 0 ? false : _c, _d = _a.actions, actions = _d === void 0 ? [] : _d, _e = _a.testId, testId = _e === void 0 ? 'block-card-forbidden-view' : _e, _f = _a.showActions, showActions = _f === void 0 ? true : _f, _g = _a.link, link = _g === void 0 ? '' : _g, _h = _a.onClick, onClick = _h === void 0 ? function () { } : _h, icon = _a.icon;
    var handleClick = function (event) {
        return handleClickCommon(event, onClick);
    };
    return (jsx(Frame, { isSelected: isSelected, testId: testId },
        jsx(Content, null,
            jsx("div", null,
                jsx(ContentHeader, { onClick: handleClick, link: link },
                    jsx(Icon, __assign({}, icon)),
                    jsx(Name, { name: jsx(FormattedMessage, __assign({}, textTitleProps)), testId: testId ? testId + "-name" : undefined })),
                jsx(Byline, { text: jsx(FormattedMessage, __assign({}, textDescriptionProps)) })),
            jsx(ContentFooter, null,
                jsx(Provider, { name: context.text, icon: context.icon }),
                showActions && jsx(ActionList, { items: actions }))),
        jsx(Thumbnail, { testId: testId ? testId + "-thumb" : undefined, src: LockImage, color: R50 })));
};
//# sourceMappingURL=ForbiddenView.js.map