import React from 'react';
import { FormattedMessage } from 'react-intl';
import { messages } from '../../messages';
export const AuthorizeAction = (handler) => ({
    id: 'connect-account',
    text: React.createElement(FormattedMessage, Object.assign({}, messages.connect_link_account_card)),
    promise: () => new Promise(resolve => resolve(handler())),
    buttonAppearance: 'primary',
});
//# sourceMappingURL=AuthorizeAction.js.map