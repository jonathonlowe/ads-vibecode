export const getLinkProps = (href, target) => ({
    href,
    rel: target === '_blank' ? 'noopener noreferrer' : null,
    target,
});
export const getButtonProps = (isDisabled) => ({
    type: 'button',
    disabled: isDisabled,
});
export const getCustomElement = (href, onClick) => {
    if (href) {
        return 'a';
    }
    if (onClick) {
        return 'button';
    }
    return 'span';
};
//# sourceMappingURL=utilities.js.map