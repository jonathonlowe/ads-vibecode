import React, { Component } from 'react';
import Avatar from '@atlaskit/avatar';
import { DropdownItem } from '@atlaskit/dropdown-menu';
class AvatarGroupItem extends Component {
    render() {
        const { avatar, onAvatarClick, testId, index } = this.props;
        const { href, ...rest } = avatar;
        const enhancedProps = this.props;
        return (React.createElement(DropdownItem, Object.assign({ isInteractive: true }, enhancedProps, { elemBefore: React.createElement(Avatar, Object.assign({}, rest, { testId: testId && `${testId}--avatar`, borderColor: "transparent", size: "small" })), href: href, onClick: (event) => {
                if (typeof onAvatarClick === 'function') {
                    onAvatarClick(event, undefined, index);
                }
            }, rel: avatar.target ? 'noopener noreferrer' : null, target: avatar.target }), avatar.name));
    }
}
export default AvatarGroupItem;
//# sourceMappingURL=AvatarGroupItem.js.map