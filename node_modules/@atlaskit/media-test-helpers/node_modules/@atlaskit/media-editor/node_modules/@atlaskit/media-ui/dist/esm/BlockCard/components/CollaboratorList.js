/** @jsx jsx */
import { jsx } from '@emotion/core';
import AvatarGroup from '@atlaskit/avatar-group';
import { mq } from '../utils';
export var CollaboratorList = function (_a) {
    var items = _a.items, _b = _a.handleAvatarClick, handleAvatarClick = _b === void 0 ? function () { } : _b, _c = _a.handleMoreAvatarsClick, handleMoreAvatarsClick = _c === void 0 ? function () { } : _c;
    if (items.length === 0) {
        return null;
    }
    return (jsx("span", { css: mq({ display: ['none', 'inherit'] }) },
        jsx(AvatarGroup, { maxCount: 4, appearance: "stack", size: "small", data: items, onAvatarClick: function (event) {
                event.stopPropagation();
                event.preventDefault();
                handleAvatarClick(event);
            }, onMoreClick: function (event) {
                event.stopPropagation();
                event.preventDefault();
                handleMoreAvatarsClick(event);
            } })));
};
//# sourceMappingURL=CollaboratorList.js.map