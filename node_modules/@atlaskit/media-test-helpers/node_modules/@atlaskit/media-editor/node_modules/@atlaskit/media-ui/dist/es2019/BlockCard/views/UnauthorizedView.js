/** @jsx jsx */
import { jsx } from '@emotion/core';
import { B50 } from '@atlaskit/theme/colors';
import { FormattedMessage } from 'react-intl';
import { Frame } from '../components/Frame';
import { Thumbnail } from '../components/Thumbnail';
import { Provider } from '../components/Provider';
import { Name } from '../components/Name';
import { Byline } from '../components/Byline';
import { ActionList } from '../components/ActionList';
import { Content } from '../components/Content';
import { messages } from '../../messages';
import { CelebrationImage } from '../utils/constants';
import { ContentHeader } from '../components/ContentHeader';
import { ContentFooter } from '../components/ContentFooter';
import { Icon } from '../components/Icon';
import { handleClickCommon } from '../utils/handlers';
const textNameProps = { ...messages.connect_link_account_card_name };
const textBylineProps = { ...messages.connect_link_account_card_description };
export const UnauthorizedView = ({ context = { text: '' }, isSelected = false, actions = [], testId = 'block-card-unauthorized-view', showActions = true, icon = {}, link = '', onClick = () => { }, }) => {
    const handleClick = (event) => handleClickCommon(event, onClick);
    return (jsx(Frame, { isSelected: isSelected, testId: testId },
        jsx(Content, null,
            jsx("div", null,
                jsx(ContentHeader, { onClick: handleClick, link: link },
                    jsx(Icon, Object.assign({}, icon)),
                    jsx(Name, { testId: testId ? `${testId}-name` : undefined, name: jsx(FormattedMessage, Object.assign({}, textNameProps, { values: { context: context.text } })) })),
                jsx(Byline, { testId: testId ? `${testId}-byline` : undefined, text: jsx(FormattedMessage, Object.assign({}, textBylineProps, { values: { context: context.text } })) })),
            jsx(ContentFooter, null,
                jsx(Provider, { name: context.text, icon: context.icon }),
                showActions && jsx(ActionList, { items: actions }))),
        jsx(Thumbnail, { src: CelebrationImage, color: B50, testId: testId ? `${testId}-thumb` : undefined })));
};
//# sourceMappingURL=UnauthorizedView.js.map