/// <reference types="react" />
import { IconProps } from '../components/Icon';
export interface NotFoundProps {
    context?: {
        icon?: React.ReactNode;
        text: string;
    };
    link?: string;
    onClick?: React.EventHandler<React.MouseEvent | React.KeyboardEvent>;
    isSelected?: boolean;
    icon: IconProps;
    testId?: string;
}
export declare const NotFoundView: ({ context, isSelected, testId, icon, link, onClick, }: NotFoundProps) => JSX.Element;
