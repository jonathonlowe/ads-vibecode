import { FileState } from '@atlaskit/media-client';
export interface FilePreview {
    src?: string;
    orientation?: number;
}
export declare const getFilePreviewFromFileState: (state: FileState) => Promise<FilePreview>;
