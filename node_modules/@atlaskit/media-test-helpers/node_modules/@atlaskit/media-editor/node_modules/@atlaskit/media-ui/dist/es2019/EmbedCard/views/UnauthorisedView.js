import React from 'react';
import { EmbedCardUnresolvedView } from './UnresolvedView';
import { CelebrationImage } from '../../BlockCard/utils/constants';
import { ExpandedFrame } from '../components/ExpandedFrame';
import { ImageIcon } from '../components/ImageIcon';
export const EmbedCardUnauthorisedView = ({ link, context, isSelected, testId = 'embed-card-unauthorized-view', onAuthorise, inheritDimensions, onClick, }) => {
    const icon = context && context.icon && (React.createElement(ImageIcon, { src: typeof context.icon === 'string' ? context.icon : undefined }));
    return (React.createElement(ExpandedFrame, { href: link, icon: icon, text: context && context.text, isVisible: true, isSelected: isSelected, inheritDimensions: inheritDimensions, onClick: onClick, testId: testId },
        React.createElement(EmbedCardUnresolvedView, { image: CelebrationImage, title: "connect_link_account_card_name", description: "connect_link_account_card_description", context: context && context.text, button: {
                appearance: 'primary',
                text: 'connect_link_account_card',
            }, onClick: onAuthorise, testId: testId })));
};
//# sourceMappingURL=UnauthorisedView.js.map