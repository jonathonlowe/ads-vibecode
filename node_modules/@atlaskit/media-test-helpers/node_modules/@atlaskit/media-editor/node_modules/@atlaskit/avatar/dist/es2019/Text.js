import { css, jsx } from '@emotion/core';
import { N200, N900 } from '@atlaskit/theme/colors';
const Text = ({ secondary, children, truncate }) => (jsx("span", { css: css `
      display: block;
      margin: 0;
      color: ${N900};

      ${truncate &&
        `
        overflow-x: hidden;
        text-overflow: ellipsis;
        white-space: nowrap;
      `}

      ${secondary &&
        `
          color: ${N200};
          font-size: 0.85em;
      `}
    ` }, children));
export default Text;
//# sourceMappingURL=Text.js.map