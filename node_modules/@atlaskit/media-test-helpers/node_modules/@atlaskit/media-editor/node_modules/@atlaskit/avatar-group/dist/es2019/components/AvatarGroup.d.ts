import React, { Component } from 'react';
import { AvatarPropTypes } from '@atlaskit/avatar';
import { AvatarGroupOverrides, AvatarGroupProps, DeepRequired } from './types';
export default class AvatarGroup extends Component<AvatarGroupProps> {
    static defaultProps: {
        appearance: string;
        avatar: React.FunctionComponent<AvatarPropTypes>;
        showMoreButtonProps: {};
        size: string;
    };
    getOverrides(): DeepRequired<AvatarGroupOverrides>;
    renderMoreDropdown(max: number, total: number): JSX.Element | null;
    render(): JSX.Element;
}
