import React from 'react';
import { EmbedCardUnresolvedView } from './UnresolvedView';
import { LockImage } from '../../BlockCard/utils/constants';
import { ExpandedFrame } from '../components/ExpandedFrame';
import { ImageIcon } from '../components/ImageIcon';
export const EmbedCardForbiddenView = ({ link, context, isSelected, testId = 'embed-card-forbidden-view', onAuthorise, inheritDimensions, onClick, }) => {
    const icon = context && context.icon && (React.createElement(ImageIcon, { src: typeof context.icon === 'string' ? context.icon : undefined }));
    return (React.createElement(ExpandedFrame, { href: link, icon: icon, text: context && context.text, isVisible: true, isSelected: isSelected, inheritDimensions: inheritDimensions, onClick: onClick, testId: testId },
        React.createElement(EmbedCardUnresolvedView, { image: LockImage, title: "invalid_permissions", description: "invalid_permissions_description", context: context && context.text, button: {
                appearance: 'default',
                text: 'try_another_account',
            }, onClick: onAuthorise, testId: testId })));
};
//# sourceMappingURL=ForbiddenView.js.map