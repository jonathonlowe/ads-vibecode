/** @jsx jsx */
import { jsx } from '@emotion/core';
import { gs, mq } from '../utils';
export const ContentFooter = ({ children, hasSpaceBetween = false, }) => (jsx("div", { css: mq({
        display: 'flex',
        flexDirection: ['column', 'row'],
        flexGrow: [1, 'unset'],
        justifyContent: [
            hasSpaceBetween ? 'space-between' : 'flex-end',
            'space-between',
        ],
        alignItems: ['unset', 'center'],
        marginTop: [gs(1), gs(1.5)],
    }) }, children));
//# sourceMappingURL=ContentFooter.js.map