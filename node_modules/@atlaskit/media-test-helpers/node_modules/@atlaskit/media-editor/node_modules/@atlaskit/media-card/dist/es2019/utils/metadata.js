export const extendMetadata = (state, metadata) => {
    const { id } = state;
    const currentMediaType = metadata && metadata.mediaType;
    if (state.status !== 'error') {
        return {
            id,
            name: state.name,
            size: state.size,
            mimeType: state.mimeType,
            createdAt: state.createdAt,
            mediaType: currentMediaType && currentMediaType !== 'unknown'
                ? currentMediaType
                : state.mediaType,
        };
    }
    else {
        return {
            id,
        };
    }
};
//# sourceMappingURL=metadata.js.map