import { range } from 'rxjs/observable/range';
import { map } from 'rxjs/operators/map';
export var slicenator = function (blob, _a) {
    var chunkSize = _a.size;
    var totalChunks = Math.ceil(blob.size / chunkSize);
    return range(0, totalChunks).pipe(map(function (index) {
        return blob.slice(index * chunkSize, (index + 1) * chunkSize);
    }));
};
//# sourceMappingURL=slicenator.js.map