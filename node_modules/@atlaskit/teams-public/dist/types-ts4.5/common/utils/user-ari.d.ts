import { type UserARI } from '../types';
export declare const isUserARI: (ari: string) => ari is `ari:cloud:identity::user/${string}`;
export declare const toUserId: (ari: UserARI | string) => string;
