import { useAvailableSites } from '../useAvailableSites';
export var useCloudIdToUrl = function useCloudIdToUrl(cloudId, gatewayBaseUrl) {
  var _useAvailableSites = useAvailableSites({
      gatewayBaseUrl: gatewayBaseUrl
    }),
    data = _useAvailableSites.data,
    loading = _useAvailableSites.loading,
    error = _useAvailableSites.error;
  var filterData = data.filter(function (site) {
    return site.cloudId === cloudId;
  });
  if (filterData.length > 0) {
    return {
      data: filterData[0].url,
      loading: loading,
      error: error
    };
  }
  return {
    data: undefined,
    loading: loading,
    error: error
  };
};