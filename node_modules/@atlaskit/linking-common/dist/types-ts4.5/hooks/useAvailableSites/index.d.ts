import { type AvailableSite } from './types';
export declare const useAvailableSites: ({ gatewayBaseUrl, }?: {
    gatewayBaseUrl?: string | undefined;
}) => {
    data: AvailableSite[];
    loading: boolean;
    error?: Error | undefined;
};
export declare const useAvailableSitesV2: ({ gatewayBaseUrl }: {
    gatewayBaseUrl?: string | undefined;
}) => {
    data: AvailableSite[];
    loading: boolean;
    error?: unknown;
};
