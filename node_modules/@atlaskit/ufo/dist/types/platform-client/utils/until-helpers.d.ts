import { type ExperienceData } from '../../types';
import { type UFOExperience } from '../core';
type UntilExperience = {
    experience: UFOExperience;
};
type UntilCategory = {
    category: string;
};
type UntilDefinition = UntilExperience | UntilCategory;
export type UntilAllArgs = Array<UntilDefinition>;
export declare const untilAll: (deps: UntilAllArgs) => () => (data: ExperienceData) => {
    done: boolean;
    state: import("../core/experience/experience-state").UFOExperienceStateType;
} | {
    done: boolean;
    state?: undefined;
};
export {};
