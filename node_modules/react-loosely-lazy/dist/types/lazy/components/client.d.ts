import React from 'react';
import type { ComponentProps, ComponentType } from 'react';
import { Deferred } from '../deferred';
export declare function createComponentClient<C extends ComponentType<any>>({ defer, deferred, dataLazyId, moduleId, }: {
    defer: number;
    deferred: Deferred<C>;
    dataLazyId: string;
    moduleId: string;
}): (props: ComponentProps<C>) => React.JSX.Element;
//# sourceMappingURL=client.d.ts.map