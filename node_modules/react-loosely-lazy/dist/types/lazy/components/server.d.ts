import React from 'react';
import type { ComponentProps, ComponentType } from 'react';
import { ServerLoader } from '../loader';
export declare function createComponentServer<C extends ComponentType<any>>({ dataLazyId, defer, loader, moduleId, ssr, }: {
    dataLazyId: string;
    defer: number;
    loader: ServerLoader<C>;
    moduleId: string;
    ssr: boolean;
}): (props: ComponentProps<C>) => React.JSX.Element;
//# sourceMappingURL=server.d.ts.map