import type { Context } from 'react';
import type { SubscriptionContextValue } from '../types';
type UseSubscriptionArgs<C> = {
    context: Context<C>;
    load: () => void;
    onValue: (v: number) => boolean;
};
export declare function useSubscription<C extends SubscriptionContextValue>({ context, load, onValue, }: UseSubscriptionArgs<C>): void;
export {};
//# sourceMappingURL=utils.d.ts.map