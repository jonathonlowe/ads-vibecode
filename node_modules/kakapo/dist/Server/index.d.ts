import { Router } from '../Router';
import { Database, DatabaseSchema } from '../Database';
export declare class Server<M extends DatabaseSchema> {
    readonly config: any;
    db: Database<M> | null;
    router: Router<M> | null;
    constructor(config?: any);
    use(entity: Database<M> | Router<M>): void;
    remove(entity: Database<M> | Router<M>): void;
    linkEntities(): void;
}
